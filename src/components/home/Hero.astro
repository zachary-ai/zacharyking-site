---
import Button from '../ui/Button.astro';
import Section from '../ui/Section.astro';
---

<Section id="hero" background="grid">
  <div class="text-center max-w-5xl mx-auto relative">
    <!-- Accent decoration -->
    <div class="absolute top-0 left-1/2 -translate-x-1/2 w-32 h-1 bg-gradient-to-r from-transparent via-[var(--color-accent-blue)] to-transparent opacity-50"></div>

    <div class="pt-12">
      <h1 class="mb-8 leading-tight">
        Deploy an AI swarm that runs your <span class="gradient-text">entire GTM motion</span>
      </h1>
      <p class="text-2xl md:text-3xl mb-12 leading-relaxed max-w-4xl mx-auto" style="color: var(--color-text-secondary); font-weight: 400;">
        I embed as your GTM leader for 90 days and build AI infrastructure your team keeps using after I leave. You get repeatable pipeline generation, automated prospect research, and trained reps who know exactly what to say without depending on you for every deal.
      </p>
      <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
        <Button href="https://savvycal.com/Zachary-King/b18fb593" variant="primary" size="lg">
          Book a Call
        </Button>
        <Button href="/services" variant="outline" size="lg">
          View Services
        </Button>
      </div>
    </div>

    <!-- Decorative glow effect -->
    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-[var(--color-accent-blue)] opacity-5 blur-3xl rounded-full pointer-events-none"></div>
  </div>
</Section>

<style>
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  #hero h1 {
    animation: fadeInUp 0.8s ease-out;
  }

  #hero p {
    animation: fadeInUp 0.8s ease-out 0.2s backwards;
  }

  #hero > div > div:last-child {
    animation: fadeInUp 0.8s ease-out 0.4s backwards;
  }
</style>
